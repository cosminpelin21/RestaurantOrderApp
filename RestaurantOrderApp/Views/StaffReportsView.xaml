<Window x:Class="RestaurantOrderApp.Views.StaffReportsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestaurantOrderApp.Views"
        mc:Ignorable="d"
        Title="Teatris - Inventory Reports" WindowState="Maximized" Background="#121212">
    <Grid Margin="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CRITICAL STOCK REPORT" FontSize="32" Foreground="#D4AF37" 
                   FontFamily="Georgia" HorizontalAlignment="Center" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,30">
            <TextBlock Text="Current alert threshold: " Foreground="#666"/>
            <TextBlock Text="{Binding Threshold}" Foreground="White" FontWeight="Bold" Margin="5,0"/>
            <TextBlock Text="units" Foreground="#666"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding CriticalProducts}" 
                  AutoGenerateColumns="False" Background="#1A1A1A" Foreground="Black" 
                  BorderBrush="#33D4AF37" IsReadOnly="True" RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="PRODUCT" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="CATEGORY" Binding="{Binding Category.Name}" Width="*"/>
                <DataGridTextColumn Header="REMAINING STOCK" Binding="{Binding TotalQuantity}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FF4C4C"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Content="BACK TO DASHBOARD" Click="Back_Click" 
                HorizontalAlignment="Left" Margin="0,20,0,0" Background="Transparent" 
                Foreground="#666" BorderThickness="0" Cursor="Hand"/>
    </Grid>
</Window>
